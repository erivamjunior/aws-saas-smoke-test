version: 1.0
runtime: nodejs22

build:
  commands:
    pre-build:
      - npm install
      - npx prisma generate
    build:
      - npm run build

run:
  runtime-version: 22
  command: sh -lc 'npx prisma migrate deploy && npm run start -- --hostname 0.0.0.0 --port 3000'
  network:
    port: 3000
