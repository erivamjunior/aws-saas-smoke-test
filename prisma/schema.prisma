generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model WaitlistEntry {
  id        String   @id @default(uuid()) @db.Uuid
  name      String
  email     String   @unique
  company   String?
  createdAt DateTime @default(now())
}
